# Research R.9G - Owner Review of PDF Version 4

## Reviewed evidence

The project owner explicitly reviewed
`output/pdf/market_system_status_pre_research_review_v4.pdf` after generation.
The reviewed PDF remains byte-exact at SHA-256
`02a76f6d46bc74a69b7f0b10331ae26da1d07d60934091ce9d31c0abe8cdaec9`
and binds research-state fingerprint
`6218f979610ae66562ab070b55ef2e270b4d31ef52c9ccd78c7e877f194672db`
over 242 files.

## Recorded answers

1. The project objective is confirmed.
2. The completed-work summary is confirmed.
3. The free-source-only policy is confirmed.
4. The R.9F description is confirmed.
5. The owner understands and accepts the sampled-identity limitation.
6. A constrained alternative-source policy is added: if a source is
   unavailable, inaccessible through permitted ordinary access, or fails data
   quality, suggest lawful free alternatives. If the current F&O database fails
   qualification, official NSE F&O bhavcopies for the lawfully available
   historical interval are an example candidate.
7. Only preparation of an interlock-removal proposal is authorized for the next
   task.

## Conservative interpretation of the fallback policy

The requested fallback is a source-evaluation rule, not permission to work
around legal or technical controls. Alternative sources must pass the same
provider-neutral provenance, point-in-time, identity, completeness, correction,
retention, and reproducibility gates. CAPTCHA, authentication, rate limits,
licensing restrictions, and other access controls must not be bypassed.

An available NSE bhavcopy does not by itself prove a complete historical
security population, inactive-security coverage, terminal outcomes, or stable
identity. Technical accessibility does not establish retention permission or
research fitness. Separate authorization remains required before any later
acquisition when the applicable scope requires it.

## Exact authority created

The next task may prepare a code-reviewable proposal explaining how the
deliberate production interlock could later be replaced by an exact one-use
authorization mechanism.

That task may not:

- change or remove the interlock;
- connect to SQLite;
- execute SQL or PRAGMAs;
- inspect schema, tables, indexes, columns, or rows;
- execute an audit;
- acquire alternative data;
- read market rows;
- analyze, score, simulate, or backtest;
- recommend trades;
- access a broker or trade.

The database remains `LOCATED_AND_SAMPLED_NOT_QUALIFIED`. The active interlock
remains `R9D_EXACT_PRODUCTION_ACTIVATION_IMPOSSIBLE`. A separate owner review of
the future proposal and another explicit authorization would be required before
any boundary implementation. A later exact one-use approval would still be
required before the first database connection.

`PDF_V4_REVIEWED_INTERLOCK_REMOVAL_PROPOSAL_ONLY`
